package com.example.SafeProof.repositories;

import com.example.SafeProof.models.TipoCrimeModel;
import org.springframework.data.jpa.repository.JpaRepository;


public interface TipoCrimeRepository extends JpaRepository<TipoCrimeModel, Integer> {

}
